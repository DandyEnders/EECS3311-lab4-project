game: SIMODYSSEY
	-- stores state of game
out: 
	--Prints the state of the game and any other commands

move()
	-- converts the integer it recieves, into a direction class and calls game.move_player()

-----SIMODYSSEY----

move_player(d:DIRECTION)
require is_valid_direction(d)
	moves player_entity in the direction by first creating a coordinate object using a combination of 		the player's current coordinate and the direction, second calling *move()

	
*move()

	--moves a player to a particular sector.

*is_valid_location(c:COORDINATE):BOOLEAN
	-- return galaxy.is_sector_full(c)  
		
is_valid_direction(d:DIRECTION)
	--call is_valid(

------SECTOR----
add_entity(e:ENTITY)
require not is_full	
	-- takes an entity and adds(look through quadrants query and see if QUADRANT.is_empty is true and if so, do QUADRANT.set_entity(e)) it to the sector and changes the coord of the entity to conform to 		the coordinate of the sector.

is_full:BOOLEAN
	--return true if quadrants query of SECTOR is "full".

----QUADRANT----
is_empty:BOOLEAN
	--return true if (entity attribute).conforms_to(NULL_ENTITY)
set_entity(e:ENTITY)
	--entity:=e
	
